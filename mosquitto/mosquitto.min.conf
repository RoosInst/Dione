# Config file for mosquitto
#
# See mosquitto.conf(5) for more information.
#
#
bind_address 127.0.0.1
listener 1883
protocol mqtt

listener 8883 127.0.0.1
protocol websockets
# Requires absolute path and 'npm run build' to create this directory (changes are NOT automatic, command has to be run every release)
#http_dir /Users/rbenech/Dione/dist/
# test